// Window object represents the browser's window or potentially frame, that a document is displayed
// in.

// As long as a browser window  is open, even if no document is loaded in the window,
// the window object is defined in the current model in memory.

// All global JavaScript variable, function and objects automatically become members of the window
// object.

// Window => document